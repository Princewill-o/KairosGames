import test from 'node:test';import assert from 'node:assert/strict';import {handleProfile} from './server/profile.mjs';
test('anonymous requests cannot read or write account profiles',async()=>{for(const method of ['GET','PUT']){const r=await handleProfile(new Request('https://example.com/api/profile',{method}),{});assert.equal(r.status,401);}});
test('account writes reject cross-origin and oversized payloads before database work',async()=>{const headers={'oai-authenticated-user-id':'u1','content-type':'application/json',origin:'https://evil.test'};let r=await handleProfile(new Request('https://example.com/api/profile',{method:'PUT',headers,body:'{}'}),{});assert.equal(r.status,403);headers.origin='https://example.com';r=await handleProfile(new Request('https://example.com/api/profile',{method:'PUT',headers,body:'x'.repeat(70000)}),{});assert.equal(r.status,413);});
